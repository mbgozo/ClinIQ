{
  "name": "cliniq",
  "private": true,
  "version": "0.1.0",
  "scripts": {
    "dev": "turbo dev",
    "build": "turbo build",
    "lint": "turbo lint",
    "test": "turbo test",
    "format": "turbo format"
  },
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "devDependencies": {
    "turbo": "^2.0.3",
    "typescript": "^5.6.3",
    "eslint": "^9.5.0",
    "prettier": "^3.3.2",
    "lint-staged": "^15.2.0",
    "husky": "^9.0.11",
    "ts-node": "^10.9.2",
    "cross-env": "^7.0.3"
  },
  "lint-staged": {
    "*.{ts,tsx,js,jsx,json,md}": [
      "prettier --write"
    ],
    "*.{ts,tsx,js,jsx}": [
      "eslint --fix"
    ]
  }
}
